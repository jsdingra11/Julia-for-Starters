# memory efficient way to handle array 

tup = ("Singh", 3, "IUCAA")
typeof(tup)

# access a tuple 
tup[1]

# tuple elements are immutable
tup[21] = 10

#tuple element order is immutable 
sort(dog)

#tuple size is immutable
push!(tup,3)

#destructive assignment 
(name, age, afflilation) = tup
name
age
afflilation
